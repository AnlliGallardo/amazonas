import React from 'react'

const Loading = () => {
    return (
        <div className="alert alert-primary" role="alert">
            <h1>Cargando, espere por favor.</h1>
        </div>
    )
}

export default Loading
